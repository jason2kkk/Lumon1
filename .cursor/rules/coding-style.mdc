---
description: 编码规范与回复风格
globs:
  - "**/*.py"
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.css"
alwaysApply: true
---

# 编码规范与回复风格

## 语言规范
- 所有回复、对话、说明一律使用**中文**
- 代码中的注释一律使用**中文**
- Git commit message 使用**中文**
- 变量名、函数名、类名保持英文（遵循各语言惯例）

## 注释规范
- 每个文件顶部写明**功能注释**：简述该文件的职责、核心逻辑
- 关键代码旁写上**行内注释**：解释非显而易见的业务逻辑、算法意图、边界处理
- 不写废话注释（如 `// 定义变量`、`// 返回结果`），只注释**为什么**而非**是什么**
- 复杂函数开头写一句话说明入参、出参、副作用

## Git 提交规范
- commit message 用**中文词语简要总结**，不展开描述
- 格式：`功能关键词: 变更点1、变更点2、变更点3`
- 示例：
  - `移动端适配: 背景色统一、按钮布局、标签折叠动画`
  - `报告渲染: 新增h4解析、表格移动端滚动`
  - `修复: 停止挖掘延迟、fetchHint移动端不可见`
- 不需要写 body，一行 message 即可

## 代码风格
- 前端：遵循项目现有 Tailwind + React 风格，响应式用 `max-md:` / `md:` 前缀
- 后端：遵循项目现有 FastAPI 风格，异步函数优先
- 改动尽量最小化，不做不相关的重构
